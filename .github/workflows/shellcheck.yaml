name: Shellcheck

on:
  push:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ShellCheck
        run: ./scripts/shellcheck.sh scripts/initialize.sh scripts/install-casks.sh scripts/shellcheck.sh
